vector<string> Solution::fizzBuzz(int n) {
        
        vector<string> s;
        int i;
        for(i=1;i<=n;i++)
        {
            if(i%3==0 && i%5==0 )
                s.push_back("FizzBuzz");
            else if(i%3==0)
                s.push_back("Fizz");
            else if(i%5==0)
                 s.push_back("Buzz");
            else
            {
                string x =to_string(i);;
                 s.push_back(x);
            }
        }
    return s;
}
