#include <bits/stdc++.h>
#define ll long long
#define T ll t; cin>>t; while(t--)
using namespace std;
#define speed ios_base::sync_with_stdio(0)
#define endl "\n"

void fun(int n)
{
			vector<int>b;
			b.push_back(1);
			int c=1,j,i;
			for(j=1;j<n;j++)
			{
				c = c*(n-j)/j;
				b.push_back(c);	
			}	
	for(i=0;i<b.size();i++)
	{
		cout<<b[i]<<" ";
	}
	cout<<endl;
	
}

int main()
{
	speed;
	T
	{
		ll n ,i,j,m;
		cin>>n;
		fun(n);
	}
return 0;
}
